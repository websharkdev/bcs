'use client'

import { GTextLogo } from "@/components/general/logo"

const Loading = () => (
    <div className="flex items-center justify-center h-screen w-full">
        <GTextLogo className="w-full max-w-[80vw] aspect-137/48 text-[#eaeaea]!"/>
    </div>
)

export default Loading